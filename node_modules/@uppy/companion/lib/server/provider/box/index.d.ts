export = Box;
/**
 * Adapter for API https://developer.box.com/reference/
 */
declare class Box extends Provider {
    constructor(options: any);
    authProvider: string;
    client: any;
    _userInfo({ token }: {
        token: any;
    }, done: any): void;
    logout({ companion, token }: {
        companion: any;
        token: any;
    }, done: any): any;
    adaptData(res: any, username: any, companion: any): {
        username: any;
        items: any[];
    };
    _error(err: any, resp: any): any;
}
import Provider = require("../Provider");
