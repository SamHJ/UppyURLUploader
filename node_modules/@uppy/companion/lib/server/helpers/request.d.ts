/// <reference types="node" />
export function getRedirectEvaluator(rawRequestURL: any, blockPrivateIPs: any): (res: any) => boolean;
export function getProtectedHttpAgent(protocol: string, blockPrivateIPs: boolean): typeof http.Agent;
export function getURLMeta(url: string, blockLocalIPs?: boolean): Promise<{
    type: string;
    size: number;
}>;
export const FORBIDDEN_IP_ADDRESS: "Forbidden IP address";
import http = require("http");
